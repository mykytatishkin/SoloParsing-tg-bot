# SoloParsing Telegram Bot

Telegram бот для автоматизации парсинга и отправки запросов.

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd SoloParsing-tg-bot
```

### 2. Создание виртуального окружения

```bash
python3 -m venv .venv
```

### 3. Активация виртуального окружения

**macOS/Linux:**
```bash
source .venv/bin/activate
```

**Windows:**
```bash
.venv\Scripts\activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Установка браузеров Playwright

```bash
python3 -m playwright install chromium
```

### 6. Настройка конфигурации

Создайте файл `settings.json` в корне проекта:

```json
{
  "telegram_token": "YOUR_BOT_TOKEN",
  "urls": ["https://example.com"],
  "min_requests": 5,
  "max_requests": 20,
  "min_quantity": 1,
  "max_quantity": 10,
  "request_count": 10,
  "status_group_chat_id": null
}
```

## Запуск

```bash
python3 bot.py
```

## Команды бота

### Основные команды
- `/start` - Запуск бота
- `/menu` - Меню команд
- `/random_requests` - Запуск случайных запросов
- `/stop_random_requests` - Остановка случайных запросов

### Команды для отчетов
- `/test_cycle_report` - Получить отчет о цикле запросов в текущий чат (для тестирования)
- `/send_cycle_report` - Отправить отчет в группу -5049129065
- `/test_status` - Тестовая отправка статуса

### Команды для настроек
- `/set_status_group` - Установить группу для отправки статуса
- `/show_status_group` - Показать текущую группу для статуса
- `/set_min_requests` - Установить минимальное количество запросов
- `/set_max_requests` - Установить максимальное количество запросов
- `/set_min_quantity` - Установить минимальное количество товара
- `/set_max_quantity` - Установить максимальное количество товара
- `/set_request_count` - Установить количество запросов

## Автоматическая отправка отчета

Бот автоматически отправляет отчет о цикле запросов в группу **-5049129065** каждый день в **7:00 по Киеву**.

Формат отчета:
```
📊 Статус цикла запросов

🕐 Запуск цикла: [дата и время] (Киев)

✅ Выполнено запросов: X из Y

⏰ Следующее обновление: [дата и время] (Киев)
```

## Примечания

- **Виртуальное окружение `.venv` не должно быть в Git** - он уже добавлен в `.gitignore`
- После клонирования репозитория нужно создать виртуальное окружение и установить зависимости
- Бот должен быть запущен для автоматической отправки отчета в 7:00 утра
- Для работы Playwright требуется установка браузеров через `playwright install chromium`

## Требования

- Python 3.12+
- Виртуальное окружение (рекомендуется)
- Telegram Bot Token
- Доступ к интернету

## Зависимости

Все зависимости указаны в `requirements.txt`:
- python-telegram-bot
- playwright
- selenium
- pandas
- openpyxl
- apscheduler
- python-dotenv

## Лицензия

[Укажите лицензию]
